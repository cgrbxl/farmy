# Implementation replacement

[UC-008](uc008/README.md) runs two Knowledge instances, substitutes a separate domain provider, and explicitly rebuilds its private state through existing public contracts. The tested boundary is exact-source evidence in a local synthetic composition.
